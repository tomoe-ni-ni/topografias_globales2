export enum RolUsuario {
  admin = "admin",
  client = "client",
}

export enum EstadoUsuario {
  activo = "activo",
  inactivo = "inactivo",
}

//TODO:REVISAR
export enum TipoDocumento {
  planos = "planos",
  catastrada = "catastro",
}

export enum Permisos{
  crear = "crear",
  leer = "leer",
  borrar = "borrar",
  editar = "editar",
}